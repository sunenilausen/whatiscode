= simple_form_for(@article) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :key
    = f.input :body, input_html: { class: 'markdown-editor' }

  .form-actions
    = f.button :submit
